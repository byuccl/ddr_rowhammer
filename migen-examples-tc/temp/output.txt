Using generated target files in: ../../build/arty
usage: hw_rowhammer.py [-h] [--nrows NROWS] [--bank BANK] [--column COLUMN]
                       [--start-row START_ROW] [--read_count READ_COUNT]
                       [--read_count_range READ_COUNT_RANGE READ_COUNT_RANGE READ_COUNT_RANGE]
                       [--hammer-only HAMMER_ONLY HAMMER_ONLY] [--no-refresh]
                       [--pattern {all_0,all_1,01_in_row,01_per_row,rand_per_row}]
                       [--row-pairs {sequential,const,random}]
                       [--const-rows-pair CONST_ROWS_PAIR [CONST_ROWS_PAIR ...]]
                       [--all-rows] [--row-jump ROW_JUMP] [--plot]
                       [--payload-executor] [-v] [--srv]
                       [--experiment-no EXPERIMENT_NO]
                       [--data-inversion DATA_INVERSION DATA_INVERSION]
                       [--exit-on-bit-flip] [--log-dir LOG_DIR]

options:
  -h, --help            show this help message and exit
  --nrows NROWS         Number of rows to consider
  --bank BANK           Bank number
  --column COLUMN       Column to read from
  --start-row START_ROW
                        Starting row (range = (start, start+nrows))
  --read_count READ_COUNT
                        How many reads to perform for single address pair
  --read_count_range READ_COUNT_RANGE READ_COUNT_RANGE READ_COUNT_RANGE
                        Range of how many reads to perform for single address
                        pair in a set of tests, given as [start] [stop] [step]
  --hammer-only HAMMER_ONLY HAMMER_ONLY
                        Run only the Rowhammer attack
  --no-refresh          Disable refresh commands during the attacks
  --pattern {all_0,all_1,01_in_row,01_per_row,rand_per_row}
                        Pattern written to DRAM before running attacks
  --row-pairs {sequential,const,random}
                        How the rows for subsequent attacks are selected
  --const-rows-pair CONST_ROWS_PAIR [CONST_ROWS_PAIR ...]
                        When using --row-pairs constant
  --all-rows            Run whole test sequence on all rows. Optionally, set
                        --row-jump and --start-row
  --row-jump ROW_JUMP   Jump between rows when using --all-rows
  --plot                Plot errors distribution
  --payload-executor    Do the attack using Payload Executor (1st row only)
  -v, --verbose         Be more verbose
  --srv                 Start LiteX server
  --experiment-no EXPERIMENT_NO
                        Run preconfigured experiment #no
  --data-inversion DATA_INVERSION DATA_INVERSION
                        Invert pattern data for victim rows (divisor, mask)
  --exit-on-bit-flip    Exit tests as soon as bitflip is found
  --log-dir LOG_DIR     Directory for output files. If not given, the output
                        files (e.g. error summary) won't be written
